import logo from '../../images/logo.svg'

const Header = () => {
    return (
        <header style={{
            paddingBottom: '30px'
        }}>
            <img 
                src={logo}
            />
        </header>)
}

export default Header